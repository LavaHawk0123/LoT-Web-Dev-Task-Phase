const gal_image = document.getElementsByClassName('gallery');
const image = document.querySelector('#showcase-pic');
const anchor = gal_image.firstElementChild;
Array.from(gal_image).forEach(function(item){ //Convert the titles into an array
	console.log(anchor);
})

//gal_image.addEventListener("mouseover",function(e){
//	if(anchor)

//})
